@model IEnumerable<BcaoCuoiKy.Models.LoaiSP>

@{
    ViewBag.Title = "Index";
}

<h2>DANH SÁCH LOẠI SẢN PHẨM</h2>

<p>
    @Html.ActionLink("Thêm mới loại sản phẩm", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiSP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiSP)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaLoaiSP }) |
            @Html.ActionLink("Xem chi tiết", "Details", new { id=item.MaLoaiSP }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaLoaiSP })
        </td>
    </tr>
}

</table>
